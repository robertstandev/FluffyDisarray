using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ClashModeManager : MonoBehaviour
{
    //Aici se va tine cont de scor (de cate ori a murit fiecare si daca mai e timp valabil sau s-a terminat runda)

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
